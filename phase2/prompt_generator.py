"""
phase2/prompt_generator.py

CORE HARI FACE — ChatGPT 投稿生成プロンプトジェネレーター

役割:
  Claudeが出力した「CORE HARI 投稿情報」を受け取り、
  ChatGPTへ渡す高品質なプロンプトを生成する。

出力一覧:
  ① Instagramカルーセル（7枚スライド）
  ② キャプション
  ③ CTA 3案
  ④ Threads版
  ⑤ X版
  ⑥ 30秒リール台本
  ⑦ 60秒リール台本
  ⑧ Canva 画像テキスト（スライド別）
  ⑨ タイトル 5案
  ⑩ ハッシュタグ
  ⑪ AI自己採点

使い方:
  from phase2.prompt_generator import build_prompt
  prompt = build_prompt(handoff)
  # prompt をそのまま ChatGPT に貼り付ける

【2026-07-07(1回目): 新規作成。Phase2 ChatGPT プロンプトジェネレーター。】
"""

from __future__ import annotations

# ── ブランド定義（固定） ───────────────────────────────────────────────────────

_BRAND_PROFILE = """
## ブランドプロフィール: CORE HARI FACE

- サロン: 顔専門エステ（小顔矯正・顔筋トレーニング・たるみ改善）
- 場所: 札幌
- オーナー: 森このみ（一人称で語る）
- 対象: 30〜40代女性（顔の変化が気になり始めた方）

## ブランドトーン（必ず守る）
- やさしい・専門的・押し付けない
- 「プロが教えます」という上から目線は禁止
- 施術を前提にしない。セルフケア中心。
- 「今日からできる」「道具不要」「30秒でできる」を優先
- 美容雑誌の文体禁止（「〜で美肌に」「〜の秘密」等）
- 読者が「知らなかった」「これ私だ」「保存しておきたい」と思える内容

## 語り口（森このみの話し方）
- 断定する:「〜です」「〜があります」「〜になります」
- 思い込みを壊す:「順番が逆です」「実はこちらが先です」
- 現場の観察を語る:「毎日顔を見ていて気づいたことがあります」
- 読者に話しかける:「あなたも同じですか？」「一度試してみてください」
- 口語で自然に:書き言葉ではなく、話し言葉のリズム
"""

# ── CORE HARI 専門視点（固定） ───────────────────────────────────────────────

_CORE_HARI_PERSPECTIVES = """
## CORE HARI が見る場所（必ず1つ以上投稿に入れる）
咬筋 / 噛み癖 / 舌の位置 / 姿勢 / 首 / デコルテ /
呼吸 / 表情グセ / 左右差 / 目元 / 頭皮 / 横向き寝 /
交感神経 / 副交感神経 / 顔の使い方 / 笑い方
"""

# ── 絶対NG（固定） ────────────────────────────────────────────────────────────

_NG_RULES = """
## NG（絶対に書かない）
- 医学的断定:「〇〇が増える」「〇〇が破壊される」等
- エビデンスのない因果:「夏だから筋肉が弱る」等
- 一般論だけで終わる:「マッサージしましょう」「保湿しましょう」だけ
- 上から目線:「プロが教えます」「専門家監修」
- 施術前提:「サロンに来ると」「施術を受けると」
- 過剰な絵文字（1スライド1〜2個まで）
- 「〜することが大切です」だけで終わるスライド（行動を促すか、発見を提供する）
"""

# ── スライドテンプレート定義（固定） ──────────────────────────────────────────

_CAROUSEL_TEMPLATE = """
## Instagramカルーセル 7枚構成（固定フォーマット）

スライド1【Hook】
  - 選択したHookをそのまま使う（一字も変えない）
  - 背景: シンプル / 文字: 大きく / 読む気になる一文

スライド2【共感】
  - お客様が実際によく言う言葉・悩みを再現する
  - 「こういう方、多いんです」という切り出し
  - 読者が「これ私だ」と思える具体性

スライド3【CORE HARI視点】
  - 「私はまず〇〇を見ます」という切り口
  - 〇〇 = Expert Angleで選んだCORE HARI視点
  - 「なぜここを見るのか」の一言説明を添える

スライド4【理由・発見】
  - なぜCORE HARIはそこにアプローチするのか
  - 現場で見てきた観察を「毎日顔を見ていて〜」と語る
  - 一般論（血流・リンパ）だけで終わらない
  - CORE HARIらしい根拠（印象・習慣・顔の使い方ベース）

スライド5【今日できるセルフケア】
  - 道具不要・30秒以内・今すぐできる
  - CORE HARIらしいアプローチ必須
    例:「舌を上あごにつける」「咬筋を軽くほぐす前に噛む回数を減らす」
  - 「まずマッサージしましょう」だけで終わらない

スライド6【変化・結果のイメージ】
  - 続けると何が変わるかを、印象・見え方で伝える
  - 数値や医学的根拠は不要。「〜に見えるようになります」でOK
  - 読者が「やってみたい」と思えるイメージ

スライド7【CTA・まとめ】
  - 「保存して後で見返してください」または「試してみてください」
  - 投稿目的に応じて変える（下記参照）
  - アカウントフォローを促す一言（押しつけなし）
"""

# ── 投稿目的別 CTA ガイド ─────────────────────────────────────────────────────

_CTA_GUIDE = """
## 投稿目的別 CTA ガイド

保存型: 「保存して、今日から試してみてください。」
共感型: 「同じ悩みがある方、ぜひ教えてください。」
信頼型: 「気になることがあれば、コメントください。」
行動型: 「今日から1つだけやってみてください。」
予約型: 「まず自分でできることから始めてみてください。もし変化を感じたら、プロの施術も選択肢の一つです。」
"""

# ── Threads フォーマット ───────────────────────────────────────────────────────

_THREADS_GUIDE = """
## Threads版ガイド

- 1投稿で完結する（カルーセルの要約ではなく、独立した投稿）
- 200〜400文字程度
- 冒頭1行でスクロールを止める（Hookと同じ切り口で別の言い回し）
- 「改行」を多用して読みやすく
- 最後に問いかけ or 共感を求める一言
- ハッシュタグは最後に2〜3個だけ
"""

# ── X フォーマット ────────────────────────────────────────────────────────────

_X_GUIDE = """
## X（旧Twitter）版ガイド

- 140文字以内（日本語）
- 冒頭でスクロールが止まる一文（Hookの圧縮版）
- 短くても「発見」または「共感」が一つ入る
- URLは含めない（画像投稿を想定）
- ハッシュタグは1〜2個だけ
"""

# ── リール台本フォーマット ────────────────────────────────────────────────────

_REEL_GUIDE = """
## リール台本ガイド（共通ルール）

- 話し言葉で書く（読み上げやすいリズム）
- 一文を短く（15〜20文字程度）
- 専門用語は一度だけ使い、すぐ噛み砕く
- 森このみが実際に話しているようなテンポ
- BGMが入ることを想定（静かなシーンあり）

### 30秒リール台本（約150〜180文字）
構成:
  0〜5秒:  冒頭Hook（スクロールを止める一言）
  6〜20秒: 問題提示 or 共感（「こういう方いませんか？」）
  21〜28秒: CORE HARI視点からのアドバイス（1点だけ）
  29〜30秒: CTA（「保存してみてください」等）

### 60秒リール台本（約350〜400文字）
構成:
  0〜5秒:  冒頭Hook
  6〜20秒: 問題提示・共感
  21〜40秒: CORE HARI視点の説明（「私はまず〇〇を見ます」）
  41〜55秒: 今日からできるセルフケア（1〜2点）
  56〜60秒: CTA
"""

# ── Canva テキストガイド ──────────────────────────────────────────────────────

_CANVA_GUIDE = """
## Canva 画像テキストガイド（スライド別）

各スライドのCanva画像に入れるテキストを以下の形式で出力:

  スライドN:
    メインテキスト: 〔大きく見せる一言〕
    サブテキスト:   〔小さく添える補足〕（省略可）
    備考:           〔デザイン指示〕（例: 背景白・文字黒・強調色など）

ルール:
- メインテキストは1スライド1〜2行まで
- 長い文章は入れない（Canvaに収まるサイズを意識）
- スライド1（Hook）は特に大きく・インパクト重視
- スライド7（CTA）はアカウント名・QRコードの余白を確保
"""

# ── 採点ルーブリック ──────────────────────────────────────────────────────────

_SCORING_RUBRIC = """
## AI自己採点ルーブリック（100点満点）

最後に以下の観点で採点し、合計点と改善コメントを出力すること。

| 観点 | 配点 | チェックポイント |
|---|---|---|
| Hook力 | 20点 | スクロールが止まるか。続きを読みたいか。 |
| CORE HARI独自性 | 20点 | 一般的な美容アカウントにない視点が入っているか。 |
| 共感・共通点 | 15点 | 読者が「これ私だ」と思えるか。 |
| セルフケアの具体性 | 15点 | 今日からできる行動が明確か。道具不要か。 |
| NGルール遵守 | 15点 | 医学的断定・一般論で終わるスライド・施術前提がないか。 |
| 全体の一貫性 | 10点 | Hook→共感→視点→理由→ケア→変化→CTAの流れが自然か。 |
| ブランドトーン | 5点 | 森このみが実際に言いそうな言葉か。雑誌文体でないか。 |

採点後に:
- 最も良かった点 1つ
- 改善すべき点 1〜2つ
を必ず出力すること。
"""

# ── プロンプト本体生成 ────────────────────────────────────────────────────────

def build_prompt(handoff: dict) -> str:
    """
    CORE HARI 投稿情報（handoff dict）からChatGPT用プロンプトを生成する。

    handoff キー:
      hook, post_type, reality, world_context, topic,
      expert_angles (list[str]), ref_instagrams (list[dict])

    Returns:
      ChatGPTに貼り付けるプロンプト文字列
    """
    hook           = handoff.get("hook", "")
    post_type      = handoff.get("post_type", "")
    reality        = handoff.get("reality", "なし")
    world_context  = handoff.get("world_context", "")
    topic          = handoff.get("topic", "")
    expert_angles  = handoff.get("expert_angles", [])
    ref_instagrams = handoff.get("ref_instagrams", [])

    # Expert Angle を箇条書きに
    expert_str = "\n".join(f"・{a}" for a in expert_angles) if expert_angles else "（未選択）"

    # 参考Instagram を整形
    ref_str = ""
    if ref_instagrams:
        for i, r in enumerate(ref_instagrams, 1):
            ref_str += f"\n  参考{i}: {r.get('url', '')}"
            if r.get("hook"):
                ref_str += f"\n         Hook: {r['hook']}"
            if r.get("likes"):
                ref_str += f" / いいね:{r['likes']}"
    else:
        ref_str = "\n  （なし）"

    # ── プロンプト本体 ──────────────────────────────────────────────────────
    prompt = f"""\
# CORE HARI FACE — Instagram投稿生成プロンプト

以下の「CORE HARI 投稿情報」をもとに、指定された全出力を生成してください。
出力は番号順に、見出し付きで出力してください。

---
{_BRAND_PROFILE}
---
{_CORE_HARI_PERSPECTIVES}
---
{_NG_RULES}
---

## 今回の投稿情報（必ず読んでから生成する）

【Hook】
{hook}

【投稿目的】
{post_type}

【Topic】
{topic}

【Reality】
{reality}

【World Context】
{world_context if world_context else "（なし）"}

【Expert Angle（必ず投稿に入れる）】
{expert_str}

【参考Instagram（構成・Hook・切り口の参考に）】{ref_str}

---

## 生成ルール（全出力共通）

1. **Reality First**: 投稿の中身はHookではなくRealityから組み立てる。
   Realityが「なし」の場合は、Expert Angleと季節・World Contextから組み立てる。
   一般論（「血流が〜」「リンパが〜」だけ）では終わらせない。

2. **Expert Angle必須**: 上記のExpert Angleを必ず1つ以上すべての出力に含める。
   CORE HARIらしい視点がないと採点が大幅に下がる。

3. **セルフケア中心**: 施術・サロンを前提にしない。
   「今日からできる」「道具不要」「30秒」を優先。

4. **根拠のある意外性**: 因果関係は「印象・習慣・顔の使い方」ベースのみ。
   医学的断定・弱い因果（「夏だから筋肉が弱る」等）は禁止。

5. **NGルール厳守**: 上記NGリストを必ず確認してから出力する。

---
{_CAROUSEL_TEMPLATE}
---
{_CTA_GUIDE}
---
{_THREADS_GUIDE}
---
{_X_GUIDE}
---
{_REEL_GUIDE}
---
{_CANVA_GUIDE}
---

## 出力指示

以下を**番号順**に、**見出し付き**で出力してください。
途中で省略せず、すべて出力してください。

---

### ① Instagramカルーセル（7枚）

上記「Instagramカルーセル 7枚構成」のフォーマットに従い、
スライド1〜7の本文テキストを出力してください。
各スライドに「スライドN【役割名】」の見出しをつけてください。

---

### ② キャプション

- 200〜300文字
- 冒頭1行: Hookと同じ切り口で別の言い回し（コピペ禁止）
- 本文: 投稿の補足・続きを読みたくなる内容
- 末尾: 「プロフィールから他の投稿もどうぞ」等のナチュラルな誘導
- ハッシュタグは含めない（⑩で別途出力）

---

### ③ CTA 3案

投稿目的「{post_type}」に合わせた CTA を3パターン出力してください。
各CTAに1行のコメント（どんな場面に向くか）を添えてください。

---

### ④ Threads版

上記「Threads版ガイド」に従い、独立した投稿として出力してください。
カルーセルの要約ではなく、Threads単体で完結する内容にしてください。

---

### ⑤ X版

上記「X版ガイド」に従い、140文字以内で出力してください。
文字数を括弧書きで末尾に表示してください。

---

### ⑥ 30秒リール台本

上記「リール台本ガイド」の30秒構成に従い、台本形式で出力してください。
各パートに時間目安（例: 0〜5秒）を記載してください。

---

### ⑦ 60秒リール台本

上記「リール台本ガイド」の60秒構成に従い、台本形式で出力してください。
各パートに時間目安を記載してください。

---

### ⑧ Canva 画像テキスト（スライド別）

上記「Canva 画像テキストガイド」に従い、
スライド1〜7それぞれのメインテキスト・サブテキスト・備考を出力してください。

---

### ⑨ タイトル 5案

この投稿のSEO・保存を意識したタイトルを5案出力してください。
形式: 「タイトル」（想定読者・効果の一言コメント）

---

### ⑩ ハッシュタグ

以下のカテゴリ別に出力してください:
- 大カテゴリ（100万件以上）: 3個
- 中カテゴリ（10〜100万件）: 5個
- 小カテゴリ（〜10万件、ニッチ）: 5個
- CORE HARI専用（ブランド・サロン名）: 3個

合計 16個。重複なし。#をつけて出力。

---

### ⑪ AI自己採点

上記「AI自己採点ルーブリック」に従い、
各観点の点数・理由・合計点を表形式で出力してください。
最後に「最も良かった点」と「改善すべき点」を必ず書いてください。

---
{_SCORING_RUBRIC}
---

以上です。すべての出力を省略せず、番号順に出力してください。
"""
    return prompt


def print_prompt(handoff: dict) -> None:
    """プロンプトをコンソールに出力する（コピペ用）。"""
    prompt = build_prompt(handoff)
    sep = "=" * 70
    print()
    print(sep)
    print("  ChatGPT プロンプト — 以下をコピーしてChatGPTに貼り付けてください")
    print(sep)
    print()
    print(prompt)
    print(sep)
    print()


def handoff_to_dict(
    hook: str,
    post_type: str,
    reality: str,
    world_context: str,
    topic: str,
    expert_angles: list[str],
    ref_instagrams: list[dict] | None = None,
) -> dict:
    """
    creator_studio の出力から handoff dict を組み立てるヘルパー。

    使い方:
      from phase2.prompt_generator import handoff_to_dict, print_prompt
      d = handoff_to_dict(hook=..., post_type=..., ...)
      print_prompt(d)
    """
    return {
        "hook":           hook,
        "post_type":      post_type,
        "reality":        reality or "なし",
        "world_context":  world_context,
        "topic":          topic,
        "expert_angles":  expert_angles or [],
        "ref_instagrams": ref_instagrams or [],
    }
