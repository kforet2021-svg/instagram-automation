# Creator Intelligence Sprint 2 設計レビュー

作成日: 2026-06-30。実装は行わず、Sprint2着手前の設計図としてまとめたもの。今週はこの文書の確認のみとし、合意後にリセットしてから実装に入る想定。

## 0. 前提: 現状の実装状況について

今回のSprint2着手の少し前から、Research Engineの土台(`research_engine.py`)、North Star Indexの設定化(`north_star_index.py`のCOMPONENTS化)、North Star Dailyの12項目化(`prompts.py`)、knowledge_libraryの重複排除ロジック(`knowledge_registry.py`、`sheets_writer.py`の関連関数)までは実装が進んでしまっている状態。今回の依頼は「実装せず設計図だけ」とのことなので、これらのファイルにこれ以上の変更は加えていない。リセット後にこの設計図を元に実装し直す場合、これらの既存ファイルをどう扱うか(そのまま土台として使うか、設計図に合わせて作り直すか)は別途相談したい。

以下、依頼順に①〜⑤をまとめる。

## ① Research Engineを追加する最適な構成

現状の`research_engine.py`は、`ResearchProvider`という抽象基底クラスと、優先順位①Meta公式情報〜⑧論文までの8つの具象クラスを1ファイルにまとめ、`gather_evidence_for_post`という関数が呼び出し側(`prompts.py`)に根拠データを渡す、という3層構造になっている。1ファイルにまとまっているのは今のスタブ段階では問題ないが、各プロバイダが実際の検索APIと繋がり始めると、プロバイダごとに認証情報・レート制限・エラーハンドリングが個別に発生するため、1ファイルのままでは育てにくくなる。

最適な構成は、抽象クラスの置き場所(`base.py`)、各プロバイダの実装(プロバイダ1つにつき1ファイル)、優先順位や呼び出し集約の置き場所(`engine.py`)の3種類を分離することだと考える。呼び出し側(`prompts.py`)が直接importしてよいのは`engine.py`の`gather_evidence_for_post`と`build_evidence_summary_text`の2関数だけにし、個々のプロバイダクラスや`PRIORITY_ORDER`の実体は呼び出し側から直接見えないようにする。この境界を最初から守っておけば、将来9番目の情報源を追加する作業は「新しいプロバイダファイルを1つ追加して`engine.py`の一覧に1行加える」だけで完了し、呼び出し側のコードは一切変更不要になる。

合わせて、各プロバイダが返すevidence(根拠1件)の型を、現状の`{"出典名", "URL", "概要", "取得日"}`という素朴な辞書のままにせず、信頼度や鮮度による絞り込みを将来行えるよう、項目を増やしても壊れない形(辞書のキーを増やすだけで対応できる設計。dataclass化までは今はしなくてよい)にしておくとよい。

もう一点、実際の検索APIが入った段階で気をつけたいのが「同じ投稿・同じアカウントについて1回の実行で何度も同じ問い合わせをしない」というコスト制御で、これは現状の8プロバイダがスタブなので問題になっていないが、実装が入る前に「1回の実行内で同じクエリは1回しか呼ばない」ような簡易キャッシュの置き場所(`engine.py`内のメモリキャッシュで十分)を設計に含めておくと、実装時の手戻りを減らせる。

最後に、各プロバイダのON/OFFを環境変数やconfigで個別に切り替えられるようにしておくことを勧める。これは「検索方法は後から変更できるように」という要望そのものへの対応であり、かつ⑤のSaaS化(将来テナントごとに使う情報源が変わる可能性がある)にも効いてくる。

## ② Knowledge Databaseの最適な設計

現状のknowledge_libraryは、Googleスプレッドシート1シートに「成功パターン」(投稿カテゴリ×検出した心理トリガーを文字列結合した名前)をキーとして重複排除する設計にしている。この設計には2つの将来的な弱点がある。

1つ目は、重複判定のたびにシート全行を読み込んで完全一致を探す(`get_all_values()`)という方式が、行数が数百〜数千件に増えるとシート読み込みのコストと処理時間が線形に増えていくという点。Google Sheets APIのリクエスト数上限に過去に一度引っかかった経緯があることを踏まえると、knowledge_libraryが本格的に育ってきたタイミングで同じ問題が再発する可能性が高い。

2つ目は、「投稿カテゴリ×心理トリガーの文字列結合」という重複判定キーが粗く、本質的に違うパターンが同じキーに丸め込まれたり、表記のわずかな違い(順序違いなど)で同じはずのパターンが別物として登録されたりする余地があるという点。また現状は「実績」列に最高スコアだけを保持して過去の値を上書きしてしまうため、「このパターンが時間とともに伸びているか廃れているか」という履歴を後から振り返れない。

最適な設計としては、knowledge_registry.py(重複判定・更新ロジック)とシートへの読み書き(sheets_writer.py)の分離は今のままでよいが、その間に「知識ストア」という抽象(取得・更新・全件取得の3操作だけを持つインターフェース)を1枚挟んでおくことを勧める。今はその実装がGoogleスプレッドシートであっても構わないが、将来読み込みが重くなった際にSQLite等の実体に切り替える判断を、knowledge_registry.py側のロジック(重複判定・信頼度の再計算など)に手を入れずに行えるようにしておきたい。

重複判定キーについては、「成功パターン」という人間向けの表示名と、判定用の構造化キー(カテゴリ・トリガーの配列など)を分けて持たせ、表記揺れに強くしておくことを勧める。また、現状のように最新の値で上書きするのではなく、観測ごとの履歴を別シート(knowledge_library_history)に薄く積んでおくと、後から「伸びている知識/廃れている知識」を分析する際の元データになる。信頼度の閾値(1件=低、2〜3件=中、4件以上=高)も、North Star Indexの配点と同じように設定ファイル化しておくと運用しながらの調整がしやすい。

## ③ Instagram以外(TikTok・YouTube Shorts・Threads・X)へ拡張する場合の設計

現状、投稿データの取得は`bright_data_fetcher.py`がInstagram(Bright Data経由)専用の形で直接`main.py`から呼ばれており、取得した投稿は`url`・`username`・`posted_at`・`views`・`likes`・`comments`・`saves`・`shares`・`followers`・`view_multiplier`・`caption`・`hashtags`・`category`という共通フィールドを持つ辞書として後段(Trend Score・成功要因分析・North Star Daily等)に渡されている。後段の処理は基本的にこの辞書の形にしか依存していないため、プラットフォームを増やす際の核心は「取得層をどのプラットフォームでも同じ形の辞書に正規化して後段へ渡す」という1点に尽きる。

今、地味だが効果が大きい変更として、投稿の辞書に`platform`キー(値は`"instagram"`)を今のうちから追加しておくことを勧める。これを入れておけば、後でTikTok等の投稿が混在するようになった際に、シート上で「どのプラットフォームの投稿か」を区別できるし、Trend Scoreの配点をプラットフォームごとに変えたくなった場合の分岐にも使える。

Trend Scoreについては、再生数や再生倍率の意味がプラットフォームごとに異なる(例えばTikTokとYouTube Shortsでは「伸びている」の基準になる数字の規模が違う)ため、将来的には評価項目自体(`trend_score.py`)もプラットフォームごとに差し替え可能にする必要が出てくる可能性がある。ただし今すぐそこまで手を入れる必要はなく、出力フォーマット(0〜100点・breakdown辞書)を共通にしたまま、内部の採点ロジックだけをプラットフォームごとに切り替えられるよう、North Star Indexで導入したCOMPONENTS方式と同じ考え方を将来当てはめられる、という見通しだけ持っておけば十分。

一方、成功要因分析(`prompts.py`の`SUCCESS_FACTOR_*`系)やNorth Star Dailyの生成プロンプトは、すでに「テキストとしての構成・フック・CTA・心理トリガー」という抽象度で書かれているため、プラットフォームが増えてもこのレイヤーはほとんど変更不要になる見込みが高い。つまり、拡張コストが集中するのは取得層(④で述べるProvider化)であり、分析層・レポート層への影響は小さい、という整理になる。

## ④ Provider構造にした場合のディレクトリ構成

「いきなり全部移動せず、動作確認しながら進める」という方針を踏まえ、最終形のイメージと、そこへ向かう移行の入り口の両方を示す。

最終的な配置イメージ(あくまで設計図。実際の移動は複数回のセッションに分けて行う想定):

```
creator_intelligence/
  fetch/
    base.py              # PostProviderの抽象クラス(fetch(accounts) -> list[dict])
    instagram_bright_data.py   # 現bright_data_fetcher.pyの実体を移設
    tiktok_*.py           # 将来追加
    youtube_shorts_*.py    # 将来追加
    threads_*.py           # 将来追加
    x_*.py                 # 将来追加
    registry.py            # platform名 -> providerインスタンスの対応表
  research/
    base.py               # ResearchProviderの抽象クラス
    providers/
      meta_official.py
      instagram_official.py
      company_ir.py
      marketing_article.py
      note.py
      x.py
      news.py
      academic_paper.py
    engine.py             # gather_evidence_for_post / build_evidence_summary_text
  knowledge/
    store.py              # 知識ストアの抽象(get/upsert/all)
    sheets_store.py        # Googleスプレッドシート実装
    registry.py            # 重複判定・信頼度更新ロジック(現knowledge_registry.py)
  scoring/
    trend_score.py
    north_star_index.py
  reporting/
    north_star_report.py   # Markdown/HTML生成
  prompts/                # prompts.pyが大きくなり続けた場合の分割先(段階別に分割)
main.py                   # 唯一の実行入口。`python3 main.py`は変更しない
sheets_writer.py          # 1466行まで肥大化しているため、いずれドメインごとに
                           # 分割したいが、それ自体も別の移行作業として扱う
config.py
accounts.py
```

移行の入り口としては、リスクが最も低い`research_engine.py`(まだ外部依存もなく、呼び出し側も`prompts.py`の1ヶ所だけ)を最初の移設対象にし、`creator_intelligence/research/engine.py`へ実体を移したうえで、ルートに残す`research_engine.py`を「`from creator_intelligence.research.engine import *`」だけの薄い互換シムにする、という進め方を勧める。これで呼び出し側(`prompts.py`、`sheets_writer.py`)は1行も変更せずに済み、`python3 main.py`の動作確認だけで移行の成否を確認できる。1つ目の移行で問題がないことを確認したうえで、次の対象(knowledge_registry.py→knowledge/registry.pyなど)に進む、という1ファイルずつの移行を繰り返す。

`bright_data_fetcher.py`(1020行)と`sheets_writer.py`(1466行)は現状すでに大きく、Provider化・ドメイン分割の本命ではあるが、依存している箇所が多いため、上記の小さい移行で進め方が確立してから着手するのが安全だと考える。

## ⑤ 将来SaaS化する場合に今のうち直しておくべき設計

現状のコードはすべて「CORE HARI FACE 1社が、自分のための1つの設定で動かす」ことを前提に作られており、それ自体は今の使い方として正しい判断だが、SaaS化(複数の顧客が同じ仕組みを使う)を見据えるなら、特に以下の4点は今のうちに直す方がコストが低い。

1つ目は、`config.py`が起動時(import時)に環境変数を読み込み、`GOOGLE_SHEET_ID`や各APIキーをモジュールレベルの定数として保持している点。これは「1プロセス=1テナント」を前提にした作りで、複数テナントを1つのプロセスで扱う(あるいは設定をテナントごとに切り替える)ことができない。直すとしたら、設定値を「読み込んでグローバル変数に保持する」のではなく、「テナント設定を受け取って読み込む関数」に変える必要があるが、今は1社しか使っていないため、影響範囲を最小に抑えるなら「将来テナント単位の設定オブジェクトに置き換えやすいよう、`config.py`の各定数への直接参照を主要モジュールの先頭に集約しておく」という保険的な対応だけ今回は入れておく、という選択もできる。

2つ目は、`prompts.py`に「CORE HARI FACE」というブランド名や「フェイシャルエステサロン」という業種が直接文字列として埋め込まれている点。SaaS化すれば顧客ごとにブランド名・業種・トーンが変わるため、これらは将来的に「ブランド設定」という1つのオブジェクト(ブランド名・業種・トーン・応用先の説明)に切り出し、プロンプト生成関数がその設定オブジェクトを受け取る形に直す必要がある。

3つ目は、`accounts.py`のANTENNA_ACCOUNTSのような「監視対象アカウントの一覧」がPythonコードの定数として管理されている点。テナントが増えれば、この一覧はテナントごとに異なるデータになるため、コードではなくデータ(スプレッドシートの別タブ、または将来のDB)として持たせる必要がある。

4つ目は、knowledge_libraryに今後溜まっていく「成功パターンの知識」を、CORE HARI FACE専用の資産として閉じるのか、複数テナントで共有する資産(Creator Intelligenceの「世界中の成功法則を蓄積するKnowledge Company」という方針に近い発想)にするのかを、データが少ないうちに決めておきたいという点。今回②で提案した知識ストアの抽象に、最初から「テナント専用」と「共有・匿名化済み」の2層を区別するフラグを持たせておけば、後からテナントが増えた際に「これは全テナントに公開してよい知識か」を都度判断する作業を、データ移行ではなく単純な運用判断に留めることができる。

以上4点はいずれも「今すぐ全部直す」必要はなく、データやコードの量がまだ少ない今のうちに「将来の置き換えポイント」を明確にしておく、という整理に留めるのが現実的だと考える。

## まとめ: 次にやること

この設計図についてフィードバックをもらえれば、それを反映した上でリセット後にSprint2の実装を再開する。実装の優先順位としては、①Research Engineの3層分離(`base.py`/プロバイダ/`engine.py`)→④の最初の移行(research_engineの`creator_intelligence/`移設)→②のknowledge_library知識ストア抽象の導入、という順で進めるのが、リスクの低い順かつ依存関係としても自然だと考えている。③(他プラットフォーム対応)と⑤(SaaS化対応)は、今すぐ着手するものではなく「今後実装する際にこの方針を踏まえる」という設計上の備忘として扱う想定。
